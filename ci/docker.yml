mkl_docker:
  stage: deploy
  image: docker:latest
  services:
    - docker:dind
  needs:
    - cpu_avx_gcc_mkl_build
  script:
    - apk add cmake
    - cmake --preset=cpu-avx-none
    - mv *.deb build/cpu-avx-none/
    - cmake --build build/cpu-avx-none --target docker

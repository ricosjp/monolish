.docker:
  stage: deploy
  image: docker/compose:1.29.1
  services:
    - docker:dind
  script:
    - docker-compose build

mkl_docker:
  extends:
    - .docker
  needs:
    - cpu_avx_gcc_mkl_build

oss_docker:
  extends:
    - .docker
  needs:
    - cpu_avx_gcc_oss_build

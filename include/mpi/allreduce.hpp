
/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] double Allreduce(double val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] float Allreduce(float val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] int Allreduce(int val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] size_t Allreduce(size_t val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] double Allreduce_sum(double val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] float Allreduce_sum(float val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] int Allreduce_sum(int val);

/**
 * @brief MPI_Allreduce (MPI_SUM) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] size_t Allreduce_sum(size_t val);

/**
 * @brief MPI_Allreduce (MPI_PROD) for scalar. Combines values from all
 * processes and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] double Allreduce_prod(double val);

/**
 * @brief MPI_Allreduce (MPI_PROD) for scalar. Combines values from all
 * processes and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] float Allreduce_prod(float val);

/**
 * @brief MPI_Allreduce (MPI_PROD) for scalar. Combines values from all
 * processes and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] int Allreduce_prod(int val);

/**
 * @brief MPI_Allreduce (MPI_PROD) for scalar. Combines values from all
 * processes and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] size_t Allreduce_prod(size_t val);

/**
 * @brief MPI_Allreduce (MPI_MAX) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] double Allreduce_max(double val);

/**
 * @brief MPI_Allreduce (MPI_MAX) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] float Allreduce_max(float val);

/**
 * @brief MPI_Allreduce (MPI_MAX) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] int Allreduce_max(int val);

/**
 * @brief MPI_Allreduce (MPI_MAX) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] size_t Allreduce_max(size_t val);

/**
 * @brief MPI_Allreduce (MPI_MIN) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] double Allreduce_min(double val);

/**
 * @brief MPI_Allreduce (MPI_MIN) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] float Allreduce_min(float val);

/**
 * @brief MPI_Allreduce (MPI_MIN) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] int Allreduce_min(int val);

/**
 * @brief MPI_Allreduce (MPI_MIN) for scalar. Combines values from all processes
 * and distributes the result back to all processes.
 * @param val scalar value
 */
[[nodiscard]] size_t Allreduce_min(size_t val);
